<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme: la UI real la pinta Compose (theme/Theme.kt);
         esto solo fija los colores de la ventana antes de que Compose arranque. -->
    <style name="Theme.IPTVFamily" parent="android:Theme.Material.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Background -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
    </style>

    <!-- Theme for player activity (fullscreen) -->
    <style name="Theme.IPTVFamily.Player" parent="Theme.IPTVFamily">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>